<UserControl x:Class="cchay_optimicer_cs.Views.DiskPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:cchay_optimicer_cs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,24">
            <!-- Header -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Limpiador de Disco" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
                <TextBlock Text="Elimina archivos temporales acumulados, logs de sistema y caché de navegadores." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Stats Summary -->
            <UniformGrid Columns="3" Rows="1" Margin="0,0,0,20">
                <!-- Selected Card -->
                <ui:Card Margin="0,0,8,0" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="28" Foreground="#1098AD" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Seleccionados" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,2" />
                            <TextBlock x:Name="TxtSelectedCount" Text="0 / 0" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <!-- Preview Size Card -->
                <ui:Card Margin="4,0,4,0" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="Eye24" FontSize="28" Foreground="#FD7E14" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Previsualizado" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,2" />
                            <TextBlock x:Name="TxtPreviewSize" Text="0 MB" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <!-- Freed Size Card -->
                <ui:Card x:Name="CardFreed" Margin="8,0,0,0" Padding="14" Visibility="Collapsed" BorderBrush="#40C057">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="Checkmark24" FontSize="28" Foreground="#40C057" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Liberado Real" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,2" />
                            <TextBlock x:Name="TxtFreedSize" Text="0 MB" FontSize="16" FontWeight="SemiBold" Foreground="#40C057" />
                        </StackPanel>
                    </Grid>
                </ui:Card>
            </UniformGrid>

            <!-- Buttons -->
            <UniformGrid Columns="2" Rows="1" Height="48" Margin="0,0,0,20">
                <ui:Button x:Name="BtnPreview" Margin="0,0,6,0" Appearance="Secondary" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="BtnPreview_Click">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Eye24" Margin="0,0,8,0" />
                        <TextBlock x:Name="TxtBtnPreview" Text="Previsualizar Espacio" FontWeight="SemiBold" />
                    </StackPanel>
                </ui:Button>
                
                <ui:Button x:Name="BtnClean" Margin="6,0,0,0" Appearance="Primary" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="BtnClean_Click">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Delete24" Margin="0,0,8,0" />
                        <TextBlock x:Name="TxtBtnClean" Text="Iniciar Limpieza" FontWeight="SemiBold" />
                    </StackPanel>
                </ui:Button>
            </UniformGrid>

            <!-- Filter Tabs & Selection Actions -->
            <Grid Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <ui:Button x:Name="TabAll" Content="Todo" Appearance="Primary" Click="TabFilter_Click" Tag="all" Margin="0,0,6,0" />
                    <ui:Button x:Name="TabSystem" Content="Sistema" Appearance="Secondary" Click="TabFilter_Click" Tag="system" Margin="0,0,6,0" />
                    <ui:Button x:Name="TabBrowser" Content="Navegadores" Appearance="Secondary" Click="TabFilter_Click" Tag="browser" Margin="0,0,6,0" />
                    <ui:Button x:Name="TabWindows" Content="Windows" Appearance="Secondary" Click="TabFilter_Click" Tag="windows" />
                </StackPanel>
                
                <ui:Button x:Name="BtnToggleAll" Content="Deseleccionar Todo" Appearance="Transparent" Foreground="{DynamicResource AccentTextWritableBrush}" HorizontalAlignment="Right" Click="BtnToggleAll_Click" />
            </Grid>

            <!-- Clean Targets List -->
            <ItemsControl x:Name="ListTargets">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0" Checked="CheckBox_Changed" Unchecked="CheckBox_Changed" />
                                
                                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,8,0" />
                                        <Border CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="6,2,6,2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Category}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <!-- Size preview -->
                                    <TextBlock Text="{Binding SizeText}" FontSize="13" FontWeight="Bold" Foreground="#FD7E14" Visibility="{Binding SizeVisibility}" />
                                    <!-- Cleaned state -->
                                    <TextBlock Text="Limpio ✓" FontSize="12" FontWeight="SemiBold" Foreground="#40C057" Visibility="{Binding CleanedVisibility}" />
                                </StackPanel>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
