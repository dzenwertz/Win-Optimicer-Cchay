<UserControl x:Class="cchay_optimicer_cs.Views.BloatwarePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:cchay_optimicer_cs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,24">
            <!-- Header -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Eliminar Bloatware" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
                <TextBlock Text="Desinstala aplicaciones del sistema innecesarias y detiene servicios pesados para liberar recursos." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Toast Messages -->
            <TextBlock x:Name="TxtMessage" Visibility="Collapsed" Text="" Padding="12" FontSize="13" FontWeight="Medium" Background="#1A107C41" Foreground="#40C057" Margin="0,0,0,20" HorizontalAlignment="Stretch" TextAlignment="Center" />

            <!-- Integration Services -->
            <TextBlock Text="Servicios de Integración Pesados" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,10" />
            <UniformGrid Columns="2" Rows="1" Margin="0,0,0,20">
                <!-- OneDrive -->
                <ui:Card Margin="0,0,6,0" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <ui:SymbolIcon Symbol="CloudOff24" FontSize="20" Foreground="{DynamicResource AccentTextWritableBrush}" Margin="0,0,8,0" />
                                <TextBlock Text="Microsoft OneDrive" FontSize="14" FontWeight="SemiBold" />
                            </StackPanel>
                            <TextBlock Text="Desinstala por completo la sincronización de OneDrive y bloquea su inicio automático." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:Button x:Name="BtnOneDrive" Grid.Row="1" Appearance="Secondary" Content="Desactivar OneDrive" HorizontalAlignment="Stretch" Click="BtnOneDrive_Click" />
                    </Grid>
                </ui:Card>

                <!-- Widgets -->
                <ui:Card Margin="6,0,0,0" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <ui:SymbolIcon Symbol="Glance24" FontSize="20" Foreground="{DynamicResource AccentTextWritableBrush}" Margin="0,0,8,0" />
                                <TextBlock Text="Widgets de Windows" FontSize="14" FontWeight="SemiBold" />
                            </StackPanel>
                            <TextBlock Text="Remueve WebExperience, eliminando el panel de noticias y widgets flotantes que consumen CPU." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:Button x:Name="BtnWidgets" Grid.Row="1" Appearance="Secondary" Content="Eliminar Widgets" HorizontalAlignment="Stretch" Click="BtnWidgets_Click" />
                    </Grid>
                </ui:Card>
            </UniformGrid>

            <!-- UWP Section Header -->
            <Grid Margin="0,0,0,15">
                <TextBlock Text="Aplicaciones de Windows (UWP)" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                <ui:Button x:Name="BtnToggleAll" Content="Seleccionar Todo" Appearance="Transparent" Foreground="{DynamicResource AccentTextWritableBrush}" HorizontalAlignment="Right" Click="BtnToggleAll_Click" />
            </Grid>

            <!-- Bulk Uninstall Button -->
            <ui:Button x:Name="BtnUninstallSelected" 
                       Appearance="Primary" 
                       Background="#EF4444" 
                       BorderBrush="#EF4444"
                       Padding="0,12,0,12" 
                       HorizontalAlignment="Stretch"
                       Visibility="Collapsed"
                       Margin="0,0,0,20"
                       Click="BtnUninstallSelected_Click">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Delete24" Margin="0,0,8,0" />
                    <TextBlock x:Name="TxtUninstallBtn" Text="Desinstalar Aplicaciones Seleccionadas (0)" FontWeight="SemiBold" Foreground="White" />
                </StackPanel>
            </ui:Button>

            <!-- Apps List -->
            <ItemsControl x:Name="ListApps">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="12" Margin="0,0,0,8" Opacity="{Binding Opacity}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" 
                                          IsEnabled="{Binding IsCheckboxEnabled}" 
                                          VerticalAlignment="Center" 
                                          Margin="0,0,12,0" 
                                          Checked="CheckBox_Changed" 
                                          Unchecked="CheckBox_Changed" />
                                
                                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                </StackPanel>

                                <Border Grid.Column="2" CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="8,4,8,4" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusText}" FontSize="11" FontWeight="SemiBold" Foreground="{Binding StatusColor}" />
                                </Border>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
