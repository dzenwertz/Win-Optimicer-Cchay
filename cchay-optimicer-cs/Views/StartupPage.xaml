<UserControl x:Class="cchay_optimicer_cs.Views.StartupPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:cchay_optimicer_cs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,24">
            <!-- Header -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Programas de Inicio" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
                <TextBlock Text="Gestiona programas que inician con Windows para acelerar el encendido y liberar memoria." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Message Toast -->
            <TextBlock x:Name="TxtMessage" Visibility="Collapsed" Text="" Padding="12" FontSize="13" FontWeight="Medium" Background="#1A107C41" Foreground="#40C057" Margin="0,0,0,20" HorizontalAlignment="Stretch" TextAlignment="Center" />

            <!-- Stats & Refresh Cards -->
            <UniformGrid Columns="2" Rows="1" Margin="0,0,0,20">
                <!-- Count Card -->
                <ui:Card Margin="0,0,6,0" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="Desktop24" FontSize="28" Foreground="{DynamicResource AccentTextWritableBrush}" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Programas Activos" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="TxtActiveCount" Text="0 / 0" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <!-- Refresh Card -->
                <ui:Card Margin="6,0,0,0" Padding="14" Cursor="Hand" MouseLeftButtonDown="BtnRefresh_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Symbol="ArrowCounterclockwise24" FontSize="28" Foreground="#40C057" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Lista de Inicio" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="Actualizar Lista" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>
                    </Grid>
                </ui:Card>
            </UniformGrid>

            <!-- Search box -->
            <ui:TextBox x:Name="TxtSearch" 
                        PlaceholderText="Buscar programas de inicio por nombre o ruta..." 
                        Icon="Search24"
                        Margin="0,0,0,20"
                        TextChanged="TxtSearch_TextChanged" />

            <!-- Items List -->
            <TextBlock Text="Programas de Inicio Detectados" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,10,0,10" />
            
            <ItemsControl x:Name="ListStartup">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="14" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Margin="0,0,12,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,8,0" />
                                        <Border CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="6,2,6,2" VerticalAlignment="Center" Margin="0,0,4,0">
                                            <TextBlock Text="{Binding LocationLabel}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </Border>
                                        <Border CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="6,2,6,2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding BadgeText}" FontSize="10" FontWeight="Bold" Foreground="{Binding BadgeColor}" />
                                        </Border>
                                    </StackPanel>
                                    
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap">
                                        <Run Text="Ruta: " />
                                        <Run Text="{Binding CleanPath, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>

                                <ui:ToggleSwitch Grid.Column="1" IsChecked="{Binding Enabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="12,0,0,0" />
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
