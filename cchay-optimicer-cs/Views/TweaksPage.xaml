<UserControl x:Class="cchay_optimicer_cs.Views.TweaksPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:cchay_optimicer_cs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,24">
            <!-- Header -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Ajustes del Sistema" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
                <TextBlock Text="Optimiza la telemetría, el comportamiento visual y la velocidad de respuesta del sistema." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- Warning Alert -->
            <ui:Card Padding="12" BorderBrush="#FA5252" Background="#11FA5252" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon Symbol="Warning24" FontSize="24" Foreground="#FA5252" Margin="0,0,12,0" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center">
                        <Run FontWeight="Bold" Text="Advertencia:" /> Algunos ajustes requieren reiniciar el sistema o el Explorador de Windows para aplicarse por completo. Usa los ajustes marcados como 'advanced' con precaución.
                    </TextBlock>
                </Grid>
            </ui:Card>

            <!-- Filter Tabs -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <ui:Button x:Name="TabAll" Content="Todo" Appearance="Primary" Click="TabFilter_Click" Tag="all" Margin="0,0,6,0" />
                <ui:Button x:Name="TabPerformance" Content="Rendimiento" Appearance="Secondary" Click="TabFilter_Click" Tag="performance" Margin="0,0,6,0" />
                <ui:Button x:Name="TabPrivacy" Content="Privacidad" Appearance="Secondary" Click="TabFilter_Click" Tag="privacy" Margin="0,0,6,0" />
                <ui:Button x:Name="TabVisual" Content="Visuales" Appearance="Secondary" Click="TabFilter_Click" Tag="visual" Margin="0,0,6,0" />
                <ui:Button x:Name="TabGaming" Content="Gaming" Appearance="Secondary" Click="TabFilter_Click" Tag="gaming" Margin="0,0,6,0" />
                <ui:Button x:Name="TabServices" Content="Servicios" Appearance="Secondary" Click="TabFilter_Click" Tag="services" />
            </StackPanel>

            <!-- Tweaks List -->
            <ItemsControl x:Name="ListTweaks">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="14" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Margin="0,0,12,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,8,0" />
                                        <Border CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="6,2,6,2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Risk}" FontSize="10" FontWeight="Bold" Foreground="{Binding RiskColor}" />
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                </StackPanel>

                                <ui:ToggleSwitch Grid.Column="1" IsChecked="{Binding Enabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="12,0,0,0" />
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
