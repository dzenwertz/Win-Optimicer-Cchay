<UserControl x:Class="cchay_optimicer_cs.Views.RamPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:cchay_optimicer_cs.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24,28,24">
            <!-- Header -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Optimizador de RAM" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,4" />
                <TextBlock Text="Limpieza profunda de memoria usando APIs nativas de Windows para liberar recursos." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- RAM Status Card -->
            <ui:Card Padding="16" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <ui:SymbolIcon Symbol="DeveloperBoard24" FontSize="36" Foreground="#40C057" Margin="0,0,20,0" VerticalAlignment="Center" />
                    
                    <StackPanel Grid.Column="1">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Estado de la Memoria RAM" FontSize="14" FontWeight="SemiBold" />
                            <TextBlock x:Name="TxtRamPercent" Text="0%" FontSize="14" FontWeight="Bold" Foreground="#40C057" HorizontalAlignment="Right" />
                        </Grid>
                        
                        <ProgressBar x:Name="ProgRam" Minimum="0" Maximum="100" Value="0" Height="10" Foreground="#40C057" Background="{DynamicResource ControlFillColorDefaultBrush}" Margin="0,0,0,8" />
                        
                        <Grid>
                            <TextBlock x:Name="TxtRamUsed" Text="Usado: 0 GB" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                            <TextBlock x:Name="TxtRamFree" Text="Libre: 0 GB" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
                            <TextBlock x:Name="TxtRamTotal" Text="Total: 0 GB" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Right" />
                        </Grid>
                    </StackPanel>
                </Grid>
            </ui:Card>

            <!-- Smart Auto-RAM Config Card -->
            <ui:Card Padding="16" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Limpieza Automática Inteligente (Segundo Plano)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBlock Text="Optimiza la RAM automáticamente cuando se alcance un alto porcentaje de uso." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,12" />
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Activar Limpieza Automática:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="13" />
                            <CheckBox x:Name="CheckAutoRam" Click="CheckAutoRam_Click" VerticalAlignment="Center" />
                            
                            <TextBlock Text="Umbral de Activación:" VerticalAlignment="Center" Margin="28,0,10,0" FontSize="13" />
                            <ComboBox x:Name="ComboThreshold" Width="80" SelectionChanged="ComboThreshold_SelectionChanged">
                                <ComboBoxItem Content="70%" Tag="70" />
                                <ComboBoxItem Content="75%" Tag="75" />
                                <ComboBoxItem Content="80%" Tag="80" />
                                <ComboBoxItem Content="85%" Tag="85" />
                                <ComboBoxItem Content="90%" Tag="90" />
                            </ComboBox>
                        </StackPanel>

                        <TextBlock x:Name="TxtAutoRamStatus" Grid.Column="1" Text="Desactivado" VerticalAlignment="Center" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    </Grid>
                </StackPanel>
            </ui:Card>

            <!-- Clean All Button -->
            <ui:Button x:Name="BtnCleanAll" 
                       Appearance="Primary" 
                       Padding="0,12,0,12" 
                       HorizontalAlignment="Stretch"
                       Margin="0,0,0,20"
                       Click="BtnCleanAll_Click">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Flash24" Margin="0,0,8,0" />
                    <TextBlock x:Name="TxtCleanAll" Text="Limpiar Toda la RAM (5 áreas)" FontWeight="SemiBold" />
                </StackPanel>
            </ui:Button>

            <TextBlock x:Name="TxtFreedToast" Visibility="Collapsed" Text="Se liberaron 0 MB de RAM." Padding="12" FontSize="13" FontWeight="Medium" Background="#1A107C41" Foreground="#40C057" Margin="0,0,0,20" HorizontalAlignment="Stretch" TextAlignment="Center" />

            <!-- Memory Areas Title -->
            <TextBlock Text="Áreas de Memoria" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,10,0,10" />

            <!-- Areas List -->
            <StackPanel x:Name="StackAreas">
                <!-- Working Set -->
                <ui:Card Padding="14" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="Working Set (Procesos)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" />
                            <TextBlock Text="Reduce el conjunto de trabajo de todos los procesos activos liberando páginas de memoria física no utilizadas." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4" />
                            <TextBlock x:Name="TxtFreed_workingSet" FontSize="11" Foreground="#40C057" Visibility="Collapsed" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" VerticalAlignment="Center" Click="BtnCleanArea_Click" Tag="workingSet" Content="Limpiar" Padding="16,8,16,8" />
                    </Grid>
                </ui:Card>

                <!-- Standby List -->
                <ui:Card Padding="14" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="Standby List (Caché de Sistema)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" />
                            <TextBlock Text="Libera la memoria en espera que Windows reserva para páginas cargadas previamente. Ideal para juegos." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4" />
                            <TextBlock x:Name="TxtFreed_standby" FontSize="11" Foreground="#40C057" Visibility="Collapsed" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" VerticalAlignment="Center" Click="BtnCleanArea_Click" Tag="standby" Content="Limpiar" Padding="16,8,16,8" />
                    </Grid>
                </ui:Card>

                <!-- System File Cache -->
                <ui:Card Padding="14" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="System File Cache (Caché de Archivos)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" />
                            <TextBlock Text="Vacía la memoria caché de archivos del sistema de archivos de Windows." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4" />
                            <TextBlock x:Name="TxtFreed_fileCache" FontSize="11" Foreground="#40C057" Visibility="Collapsed" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" VerticalAlignment="Center" Click="BtnCleanArea_Click" Tag="fileCache" Content="Limpiar" Padding="16,8,16,8" />
                    </Grid>
                </ui:Card>

                <!-- Modified Page List -->
                <ui:Card Padding="14" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="Modified Page List (Páginas Modificadas)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" />
                            <TextBlock Text="Escribe páginas de memoria modificadas pendientes en el disco y las libera." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4" />
                            <TextBlock x:Name="TxtFreed_modified" FontSize="11" Foreground="#40C057" Visibility="Collapsed" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" VerticalAlignment="Center" Click="BtnCleanArea_Click" Tag="modified" Content="Limpiar" Padding="16,8,16,8" />
                    </Grid>
                </ui:Card>

                <!-- Combined Page List -->
                <ui:Card Padding="14" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="Combined Page List (Páginas Combinadas)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4" />
                            <TextBlock Text="Combina páginas de memoria duplicadas y libera el exceso." FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4" />
                            <TextBlock x:Name="TxtFreed_combined" FontSize="11" Foreground="#40C057" Visibility="Collapsed" />
                        </StackPanel>
                        <ui:Button Grid.Column="1" VerticalAlignment="Center" Click="BtnCleanArea_Click" Tag="combined" Content="Limpiar" Padding="16,8,16,8" />
                    </Grid>
                </ui:Card>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
